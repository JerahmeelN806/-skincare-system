import { useEffect, useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { formatNaira } from '../data/products'
import { getCartSubtotal, getCartTotalItems, useCartStore } from '../store/cartStore'

function MinusIcon() { return <svg viewBox="0 0 24 24" className="h-4 w-4" fill="none" stroke="currentColor" strokeWidth="2" aria-hidden="true"><path d="M5 12h14" /></svg> }
function PlusIcon() { return <svg viewBox="0 0 24 24" className="h-4 w-4" fill="none" stroke="currentColor" strokeWidth="2" aria-hidden="true"><path d="M12 5v14M5 12h14" /></svg> }
function TrashIcon() { return <svg viewBox="0 0 24 24" className="h-5 w-5" fill="none" stroke="currentColor" strokeWidth="1.8" aria-hidden="true"><path d="M4 7h16M10 11v6M14 11v6M9 7V4h6v3M6 7l1 13h10l1-13" strokeLinecap="round" strokeLinejoin="round" /></svg> }

export function Cart() {
  const { items, removeItem, updateQuantity } = useCartStore()
  const [paymentMethod, setPaymentMethod] = useState<'card' | 'bank' | null>(null)
  const navigate = useNavigate()
  const subtotal = getCartSubtotal(items)
  const totalItems = getCartTotalItems(items)

  useEffect(() => {
    if (window.location.hash === '#payment') {
      Array.from(document.querySelectorAll('h2')).find((heading) => heading.textContent === 'Payment Method')?.closest('section')?.scrollIntoView({ behavior: 'smooth', block: 'center' })
    }
  }, [])

  if (!items.length) {
    return <main className="grid min-h-[58vh] place-items-center bg-cream px-5 py-16"><div className="max-w-md text-center"><div className="mx-auto grid h-16 w-16 place-items-center rounded-full bg-sage/10 text-sage"><svg viewBox="0 0 24 24" className="h-7 w-7" fill="none" stroke="currentColor" strokeWidth="1.7" aria-hidden="true"><path d="M5 8h14l-1 12H6L5 8Z" /><path d="M9 9V6a3 3 0 0 1 6 0v3" /></svg></div><h1 className="mt-6 font-display text-4xl font-extrabold tracking-[-0.05em] text-ink">Your Cart is Empty</h1><p className="mt-3 leading-7 text-ink/60">Your skincare favourites are waiting for you.</p><Link to="/products" className="mt-7 inline-flex items-center gap-2 rounded-full bg-sage px-6 py-3.5 text-sm font-semibold text-white transition hover:bg-sage/90">Continue Shopping <span aria-hidden="true">→</span></Link></div></main>
  }

  const paymentOptionClass = (method: 'card' | 'bank', disabled = false) => `flex w-full items-center gap-3 rounded-xl border-2 p-4 text-left transition ${paymentMethod === method ? 'border-sage bg-sage/[0.05]' : 'border-black/10 hover:border-sage/40'} ${disabled ? 'cursor-not-allowed border-black/[0.07] bg-black/[0.025] opacity-60 hover:border-black/[0.07]' : ''}`

  return <main className="bg-cream px-5 py-12 sm:px-8 md:py-20"><div className="mx-auto max-w-[1280px]"><h1 className="font-display text-4xl font-extrabold tracking-[-0.05em] text-ink sm:text-5xl">Your Cart</h1><div className="mt-10 grid items-start gap-8 lg:grid-cols-[minmax(0,1fr)_360px]"><section className="overflow-hidden rounded-2xl bg-white shadow-sm"><div className="hidden grid-cols-[minmax(0,1fr)_150px_120px_44px] gap-4 border-b border-black/[0.08] px-6 py-4 text-xs font-semibold uppercase tracking-[0.12em] text-ink/50 md:grid"><span>Product</span><span>Quantity</span><span>Price</span><span className="sr-only">Remove</span></div>{items.map((item) => <article key={item.id} className="grid gap-4 border-b border-black/[0.08] px-4 py-5 last:border-0 sm:px-6 md:grid-cols-[minmax(0,1fr)_150px_120px_44px] md:items-center"><div className="flex min-w-0 items-center gap-4"><img src={item.image} alt={item.name} className="h-20 w-20 rounded-xl object-cover" /><div className="min-w-0"><h2 className="font-display text-base font-bold text-ink">{item.name}</h2><p className="mt-1 text-sm text-ink/55">{formatNaira(item.price)} each</p></div></div><div className="flex items-center justify-between gap-4 md:block"><span className="text-xs font-semibold uppercase tracking-[0.1em] text-ink/45 md:hidden">Quantity</span><div className="inline-flex items-center rounded-full border border-black/10"><button onClick={() => updateQuantity(item.id, item.quantity - 1)} disabled={item.quantity === 1} className="grid h-9 w-9 place-items-center rounded-full text-ink transition hover:bg-cream disabled:cursor-not-allowed disabled:opacity-35" aria-label={`Decrease ${item.name} quantity`}><MinusIcon /></button><span className="w-7 text-center text-sm font-semibold">{item.quantity}</span><button onClick={() => updateQuantity(item.id, item.quantity + 1)} className="grid h-9 w-9 place-items-center rounded-full text-ink transition hover:bg-cream" aria-label={`Increase ${item.name} quantity`}><PlusIcon /></button></div></div><div className="flex items-center justify-between md:block"><span className="text-xs font-semibold uppercase tracking-[0.1em] text-ink/45 md:hidden">Price</span><p className="font-semibold text-ink">{formatNaira(item.price * item.quantity)}</p></div><button onClick={() => removeItem(item.id)} className="justify-self-end rounded-full p-2 text-ink/45 transition hover:bg-red-50 hover:text-red-600" aria-label={`Remove ${item.name}`}><TrashIcon /></button></article>)}</section><aside className="space-y-5"><section className="rounded-2xl bg-white p-6 shadow-sm"><h2 className="font-display text-xl font-bold text-ink">Order Summary</h2><div className="mt-6 space-y-4 text-sm"><div className="flex justify-between text-ink/65"><span>Subtotal ({totalItems} {totalItems === 1 ? 'item' : 'items'})</span><span className="font-medium text-ink">{formatNaira(subtotal)}</span></div><div className="flex justify-between text-ink/65"><span>Delivery</span><span className="font-medium text-sage">Free</span></div><div className="flex justify-between border-t border-black/[0.08] pt-4 font-display text-lg font-bold text-ink"><span>Total</span><span>{formatNaira(subtotal)}</span></div></div></section><section className="rounded-2xl bg-white p-6 shadow-sm"><h2 className="font-display text-xl font-bold text-ink">Payment Method</h2><div className="mt-5 space-y-3" role="radiogroup" aria-label="Payment method"><button type="button" role="radio" aria-checked={paymentMethod === 'card'} onClick={() => setPaymentMethod('card')} className={paymentOptionClass('card')}><span className={`grid h-5 w-5 place-items-center rounded-full border-2 ${paymentMethod === 'card' ? 'border-sage' : 'border-black/30'}`} aria-hidden="true">{paymentMethod === 'card' && <span className="h-2.5 w-2.5 rounded-full bg-sage" />}</span><span><span className="block text-sm font-semibold text-ink">Card Payment</span><span className="mt-0.5 block text-xs text-ink/55">Debit / Credit card</span></span></button><button type="button" role="radio" aria-checked={paymentMethod === 'bank'} disabled className={paymentOptionClass('bank', true)}><span className={`grid h-5 w-5 place-items-center rounded-full border-2 ${paymentMethod === 'bank' ? 'border-sage' : 'border-black/20'}`} aria-hidden="true">{paymentMethod === 'bank' && <span className="h-2.5 w-2.5 rounded-full bg-sage" />}</span><span className="flex-1"><span className="block text-sm font-semibold text-ink/65">Bank Transfer</span><span className="mt-0.5 block text-xs text-ink/45">Available soon</span></span><span className="rounded-full bg-black/[0.08] px-2 py-1 text-[10px] font-bold uppercase tracking-wide text-ink/55">Soon</span></button></div>{!paymentMethod && <p className="mt-4 text-xs font-medium text-amber-700">Please select a payment method to continue</p>}<button type="button" onClick={() => paymentMethod && navigate('/checkout')} disabled={!paymentMethod} className="mt-5 w-full rounded-full bg-sage px-5 py-3.5 text-sm font-semibold text-white transition hover:bg-sage/90 disabled:cursor-not-allowed disabled:opacity-40">Proceed to Checkout</button></section></aside></div><Link to="/products" className="mt-8 inline-flex items-center gap-2 text-sm font-semibold text-sage transition hover:gap-3"><span aria-hidden="true">←</span> Continue Shopping</Link></div></main>
}
